apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-user-profile-api-db-postgres-exporter
  namespace: test-user-profile-api
spec:
  releaseName: test-user-profile-api-db-postgres-exporter
  chart:
    spec:
      chart: prometheus-postgres-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: 3.0.3
  interval: 1h
  install:
    remediation:
      retries: 3
  values:
    service:
      port: 9187
    serviceMonitor:
      enabled: true
      namespace: test-user-profile-api
      interval: 30s
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
    rbac:
      create: true
      pspEnabled: false
    serviceAccount:
      create: true
    config:
      datasource:
        host: db-insights-api-primary.test-user-profile-api.svc
        user: user-profile-api
        database: user-profile-api
        passwordSecret:
          name: test-user-profile-api-db-postgres-exporter
          key: data_source_password
        sslmode: require
      logLevel: "info"
      logFormat: "logfmt"
