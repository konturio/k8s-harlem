apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-insights-api-db-postgres-exporter
  namespace: test-insights-api
spec:
  releaseName: test-insights-api-db-postgres-exporter
  chart:
    spec:
      chart: prometheus-postgres-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: 3.0.3
  interval: 1h
  install:
    remediation:
      retries: 3
  values:
    fullnameOverride: test-insights-api-db-exporter-replica
    service:
      port: 9187
    serviceMonitor:
      enabled: true
      namespace: test-insights-api
      interval: 30s
      labels:
        role: master
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 128Mi
    rbac:
      create: true
      pspEnabled: false
    serviceAccount:
      create: true
    config:
      datasource:
        host: db-insights-api-primary.test-insights-api.svc
        user: insights-api
        database: insights-api
        passwordSecret:
          name: test-insights-api-db-postgres-exporter
          key: data_source_password
        sslmode: require
      logLevel: "info"
      logFormat: "logfmt"
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-insights-api-db-postgres-exporter-replica
  namespace: monitoring
spec:
  releaseName: test-insights-api-db-exporter-replica
  chart:
    spec:
      chart: prometheus-postgres-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: 3.0.3
  interval: 1h
  install:
    remediation:
      retries: 3
  values:
    fullnameOverride: test-insights-api-db-exporter-replica
    service:
      port: 9187
    serviceMonitor:
      enabled: true
      namespace: test-insights-api
      interval: 30s
      labels:
        role: replica
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 128Mi
    rbac:
      create: true
      pspEnabled: false
    serviceAccount:
      create: true
    config:
      datasource:
        host: db-insights-api-replicas.test-insights-api.svc
        user: insights-api
        database: insights-api
        passwordSecret:
          name: test-insights-api-db-postgres-exporter
          key: data_source_password
        sslmode: require
      logLevel: "info"
      logFormat: "logfmt"
