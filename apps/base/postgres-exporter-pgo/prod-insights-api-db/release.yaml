apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prod-insights-api-db-postgres-exporter
  namespace: prod-insights-api
spec:
  releaseName: prod-insights-api-db-postgres-exporter
  chart:
    spec:
      chart: prometheus-postgres-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: 3.0.3
  interval: 1h
  install:
    remediation:
      retries: 3
  values:
    fullnameOverride: prod-insights-api-db-exporter
    service:
      port: 9187
    serviceMonitor:
      enabled: true
      namespace: prod-insights-api
      interval: 30s
      labels:
        role: master
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 128Mi
    rbac:
      create: true
      pspEnabled: false
    serviceAccount:
      create: true
    config:
      datasource:
        host: db-insights-api-primary.prod-insights-api.svc
        user: insights-api
        database: insights-api
        passwordSecret:
          name: prod-insights-api-db-postgres-exporter
          key: data_source_password
        sslmode: require
      logLevel: "info"
      logFormat: "logfmt"
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prod-insights-api-db-postgres-exporter-replica
  namespace: monitoring
spec:
  releaseName: prod-insights-api-db-postgres-exporter-replica
  chart:
    spec:
      chart: prometheus-postgres-exporter
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
      version: 3.0.3
  interval: 1h
  install:
    remediation:
      retries: 3
  values:
    fullnameOverride: prod-insights-api-db-exporter-replica
    service:
      port: 9187
    serviceMonitor:
      enabled: true
      namespace: prod-insights-api
      interval: 30s
      labels:
        role: replica
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 100m
        memory: 128Mi
    rbac:
      create: true
      pspEnabled: false
    serviceAccount:
      create: true
    config:
      datasource:
        host: db-insights-api-replicas.prod-insights-api.svc
        user: insights-api
        database: insights-api
        passwordSecret:
          name: prod-insights-api-db-postgres-exporter
          key: data_source_password
        sslmode: require
      logLevel: "info"
      logFormat: "logfmt"
