apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-path-provisioner-regular
spec:
  replicas: 1
  selector:
    matchLabels:
      app: local-path-provisioner-regular
  template:
    metadata:
      labels:
        app: local-path-provisioner-regular
    spec:
      serviceAccountName: local-path-provisioner-service-account-regular
      containers:
        - name: local-path-provisioner
          image: rancher/local-path-provisioner:v0.0.23
          imagePullPolicy: IfNotPresent
          command:
            - local-path-provisioner
            - --debug
            - start
            - --config
            - /etc/config/config-regular.json
            - --provisioner-name
            - local-path-regular
          volumeMounts:
            - name: config-volume
              mountPath: /etc/config/
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
      volumes:
        - name: config-volume
          configMap:
            name: local-path-config-regular
